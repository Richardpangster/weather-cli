# Task 001: 实现基础天气查询功能

## 🎯 任务目标

实现一个命令行天气查询工具的基础功能：查询指定城市的当前天气。

## 📋 具体需求

### 功能要求
1. 接收城市名作为命令行参数
2. 调用 Open-Meteo API 查询天气
3. 在终端显示：城市名、温度、天气状况
4. 处理错误情况（城市不存在、网络错误）

### 命令格式
```bash
python src/cli.py Beijing
# 输出示例：
# 北京
# 温度: 25°C
# 天气: 晴朗
```

## 🔧 技术要点

### 需要实现的模块

#### 1. src/weather.py
```python
"""天气查询核心模块"""

import requests
from typing import Dict, Optional

def get_coordinates(city: str) -> Optional[tuple]:
    """根据城市名获取经纬度"""
    # 调用 geocoding-api.open-meteo.com
    pass

def get_weather(lat: float, lon: float) -> Dict:
    """根据经纬度获取天气"""
    # 调用 api.open-meteo.com
    pass

def parse_weather_code(code: int) -> str:
    """解析天气代码为中文描述"""
    # WMO Weather interpretation codes
    pass
```

#### 2. src/cli.py
```python
"""命令行入口"""

import sys
from weather import get_coordinates, get_weather, parse_weather_code

def main():
    # 解析命令行参数
    # 调用查询函数
    # 格式化输出
    pass

if __name__ == "__main__":
    main()
```

### API 参考

**地理编码 API**:
```
https://geocoding-api.open-meteo.com/v1/search?name={city}&count=1
```

**天气 API**:
```
https://api.open-meteo.com/v1/forecast?latitude={lat}&longitude={lon}&current=temperature_2m,weather_code
```

### 天气代码对照表
| 代码 | 中文 |
|------|------|
| 0 | 晴朗 |
| 1-3 | 多云 |
| 45, 48 | 雾 |
| 51-55 | 毛毛雨 |
| 61-65 | 雨 |
| 71-77 | 雪 |
| 95-99 | 雷暴 |

## ✅ 验收标准

- [ ] 可以查询 "Beijing"、"Shanghai"、"New York" 等城市的天气
- [ ] 城市不存在时显示友好错误信息
- [ ] 网络错误时有适当的错误处理
- [ ] 代码有基本的注释

## 📝 提交要求

1. 在 `src/` 目录下创建所有必要文件
2. 创建 `requirements.txt` 列出依赖（requests）
3. 测试通过后提交到 Git
4. 告诉我完成，我会进行代码审查

## 💡 提示

- 先用 `curl` 或浏览器测试 API 是否正常工作
- 使用 `requests` 库处理 HTTP 请求
- 注意异常处理（try-except）
- 可以先实现最简单的版本，再逐步完善

---

**任务创建**: 全球虾 🦐  
**优先级**: P0  
**预计时间**: 30-60 分钟
